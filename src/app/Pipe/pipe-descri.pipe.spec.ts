import { PipeDescriPipe } from './pipe-descri.pipe';

describe('PipeDescriPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDescriPipe();
    expect(pipe).toBeTruthy();
  });
});
