import { PipeCurrencyTnPipe } from './pipe-currency-tn.pipe';

describe('PipeCurrencyTnPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCurrencyTnPipe();
    expect(pipe).toBeTruthy();
  });
});
